# 5. バッチ化対応 — バッチ処理分離

AgentCore ランタイム内部の Consolidation バッチ処理を独立 Lambda として分離する。
`memory/` パッケージをトップレベルに抽出し、`agentcore/` と `batch/` の両方から共有する。

## 5.1 uv workspace セットアップ ✅

### 5.1.1 ルート pyproject.toml 作成 ✅

- uv workspace 定義: `members = ["memory", "agentcore", "batch"]`

### 5.1.2 memory パッケージ pyproject.toml 作成 ✅

- 独立 Python パッケージ、src レイアウト: `memory/src/memory/`

### 5.1.3 agentcore pyproject.toml 修正 ✅

- `memory` を workspace 依存に追加

### 5.1.4 batch pyproject.toml 作成 ✅

- `memory` を workspace 依存に追加

---

## 5.2 memory パッケージ抽出 ✅

### 5.2.1 ディレクトリ構造作成 & ファイル移動 ✅

- `agentcore/memory/` から `memory/src/memory/` に移動
- `scheduler.py` → 削除、`consolidation.py` / `mental_model_trigger.py` → `batch/` に移動

### 5.2.2 engine.py からスケジューラー依存を除去 ✅

- MemoryEngine は純粋に retain/recall/reflect のラッパー + DB プール管理のみ

### 5.2.3 旧 agentcore/memory/ ディレクトリの削除 ✅

- 全ファイル移動済みを確認後、ディレクトリ削除

---

## 5.3 batch ディレクトリ作成 ✅

### 5.3.1 consolidation.py & mental_model_trigger.py 移動 ✅

- `agentcore/memory/` → `batch/` に移動
- import パスを修正
