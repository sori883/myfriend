# バッチ処理設計

## 構成

```mermaid
graph TB
    subgraph "スケジュール"
        EB["EventBridge Rule<br/>rate(5 minutes)"]
    end

    subgraph "Lambda"
        BATCH["myfriend-batch<br/>Docker Image (Python)<br/>1024MB / 780秒"]
    end

    subgraph "障害対応"
        DLQ["SQS DLQ<br/>保持期間: 14日"]
    end

    subgraph "VPC - Isolated"
        AURORA["Aurora<br/>PostgreSQL 16.4"]
    end

    subgraph "Bedrock"
        LLM["Claude Haiku<br/>(Converse API)"]
        EMB["Titan Embed V2"]
    end

    SM["Secrets Manager"]

    EB -->|"5分間隔"| BATCH
    BATCH -->|"TCP 5432"| AURORA
    BATCH -->|"Converse / Embed"| LLM
    BATCH -->|"Embed"| EMB
    BATCH -->|"GetSecretValue"| SM
    BATCH -.->|"失敗時"| DLQ
```

## Batch Lambda

| 項目 | 値 |
|---|---|
| 関数名 | `myfriend-batch` |
| イメージ | `batch/Dockerfile` からビルド |
| メモリ | 1024MB |
| タイムアウト | 780秒（13分） |
| 配置 | VPC Isolated サブネット |
| 環境変数 | `DB_SECRET_ARN`, `DB_HOST`, `DB_NAME` |

### IAM 権限

| 権限 | リソース | 用途 |
|---|---|---|
| `bedrock:*` | `*` | Consolidation / Reflect での LLM 呼び出し |
| Secrets Manager Read | DB シークレット | DB 認証情報取得 |

## 処理内容

Batch Lambda は記憶システムの中期・長期記憶を更新する。

```mermaid
sequenceDiagram
    participant EB as EventBridge
    participant BATCH as Batch Lambda
    participant DB as Aurora
    participant BR as Bedrock

    EB->>BATCH: 5分間隔トリガー

    BATCH->>DB: 全 bank_id を取得

    loop 各 bank_id
        BATCH->>DB: 未統合 Raw Facts 取得<br/>(consolidated_at IS NULL, LIMIT 10)

        loop 各 Raw Fact
            BATCH->>DB: 関連 Observation 検索<br/>(Embedding 類似度)
            BATCH->>BR: LLM 判定<br/>(CREATE/UPDATE)
            alt CREATE
                BATCH->>BR: Embedding 生成
                BATCH->>DB: Observation 新規作成
            else UPDATE
                BATCH->>BR: Embedding 生成
                BATCH->>DB: Observation 更新 + proof_count++
            end
            BATCH->>DB: consolidated_at = NOW()
        end

        BATCH->>DB: リフレッシュ対象 Mental Model 検索
        loop 最大3件
            BATCH->>BR: Reflect パイプライン実行
            BATCH->>DB: Mental Model 更新
        end
    end

    BATCH-->>EB: 完了
```

## EventBridge スケジュール

| 項目 | 値 |
|---|---|
| ルール名 | `myfriend-batch-schedule` |
| スケジュール | `rate(5 minutes)` |
| dev 環境 | 有効 |
| stg 環境 | **無効** |
| prd 環境 | 有効 |

## DLQ (Dead Letter Queue)

| 項目 | 値 |
|---|---|
| キュー名 | `myfriend-batch-dlq` |
| メッセージ保持期間 | 14日 |

Lambda 実行失敗時にイベントが DLQ に送信される。
